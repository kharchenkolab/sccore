% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap.R
\name{heatmapSpec}
\alias{heatmapSpec}
\title{Build a native (grid) heatmap specification}
\usage{
heatmapSpec(
  x,
  column.groups = NULL,
  row.groups = NULL,
  column.annotation = NULL,
  annotation.colors = NULL,
  column.annotation.colors = list(),
  row.group.colors = NULL,
  expression.palette = (grDevices::colorRampPalette(c("grey95", "firebrick3"), space =
    "Lab"))(1024),
  labeled.row.subset = NULL,
  label.indices = NULL,
  show.row.groups = TRUE,
  show.group.legend = TRUE,
  show_heatmap_legend = FALSE,
  border = TRUE,
  row.label.font.size = 10,
  split = FALSE,
  split.gap = 0,
  annotation.grobs = NULL,
  legend.max.levels = Inf,
  legend.columns = NULL,
  s = 1,
  v = 1,
  extra = list(),
  class = character()
)
}
\arguments{
\item{x}{numeric matrix to display (rows = features, columns = observations).}

\item{column.groups}{factor grouping the columns (e.g. clusters); drives column ordering/splitting.}

\item{row.groups}{factor grouping the rows (e.g. per-cluster marker blocks).}

\item{column.annotation}{data.frame of additional categorical column annotation tracks.}

\item{annotation.colors}{named list of colour vectors for the \code{column.annotation} tracks.}

\item{column.annotation.colors}{named list of colours for the \code{column.groups} track.}

\item{row.group.colors}{named colour vector for \code{row.groups}.}

\item{expression.palette}{colour ramp (vector of colours) for the expression matrix.}

\item{labeled.row.subset}{character vector of row names to label (others left unlabelled).}

\item{label.indices}{integer row indices to label (alternative to \code{labeled.row.subset}).}

\item{show.row.groups}{logical, draw the row-group track (default: TRUE).}

\item{show.group.legend}{logical, draw the group legend (default: TRUE).}

\item{show_heatmap_legend}{logical, draw the expression colour-bar legend (default: FALSE).}

\item{border}{logical, draw cell/annotation borders (default: TRUE).}

\item{row.label.font.size}{font size for row labels (default: 10).}

\item{split}{logical, split the heatmap by \code{column.groups} (default: FALSE).}

\item{split.gap}{gap (in points) between splits (default: 0).}

\item{annotation.grobs}{optional list of extra grobs placed around the heatmap (top/right/bottom/left).}

\item{legend.max.levels}{maximum number of levels shown per legend (default: Inf).}

\item{legend.columns}{number of columns in the group legend (default: NULL, auto).}

\item{s, v}{saturation and value (HSV) for generated annotation colours (default: 1, 1).}

\item{extra}{named list of extra metadata carried on the returned spec (used by callers).}

\item{class}{extra class(es) to prepend to the returned spec object.}
}
\value{
a \code{sccoreHeatmapSpec} list describing the heatmap, to pass to \code{\link{drawHeatmap}}.
}
\description{
Assemble a self-contained description of a matrix heatmap -- the colour matrix, column/row group
annotations, legends and layout -- that \code{\link{drawHeatmap}} renders with base \code{grid}
(no ComplexHeatmap dependency). Shared by pagoda2 and conos for marker-gene heatmaps.
}
